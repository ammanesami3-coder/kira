/**
 * Booking PDF generation with @react-pdf/renderer (pure JS, Vercel-safe).
 *
 * Phase 0: intentionally empty. Templates live in `components/pdf/` and are
 * rendered to a buffer here, then uploaded to Supabase Storage.
 */

export {};
