/**
 * Write-side Server Actions.
 *
 * Phase 0: intentionally empty. Booking creation (via the admin/service-role
 * client, inside a transaction that re-validates availability) is added later.
 */

export {};
