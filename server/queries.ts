/**
 * Read-side data access for Server Components.
 *
 * Phase 0: intentionally empty. Catalog/availability/settings queries are
 * added once the database schema exists (later phase).
 */

export {};
