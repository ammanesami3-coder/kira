/**
 * Availability logic (date-range overlap checks).
 *
 * Phase 0: intentionally empty. Backed later by the DB exclusion constraint
 * and the `is_car_available` function / `car_unavailable_ranges` view.
 */

export {};
